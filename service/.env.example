# 环境变量示例文件
# 复制本文件为 .env.development / .env.production 后按需修改
# 真实 env 文件不提交到 git（见 .gitignore）

# 运行环境标识
NODE_ENV=development

# 服务端口
PORT=3001

# 数据库连接
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=test

# 数据库连接池
DB_POOL_MAX=10
DB_POOL_MIN=0
